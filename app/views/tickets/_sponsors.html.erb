<div class = "sponsors container">
  <h3>With thanks to our generous sponsors...</h3>

  <div class="row top-sponsor"> 
    <img src="<%= @sponsors[0].image_url %>">
  </div>

  <div class="second-row-sponsors">
    <% @sponsors[1..3].each do |sponsor| %>
      <img src="<%= sponsor.image_url %>" alt="">
    <% end %>
  </div>
 

  


</div>
